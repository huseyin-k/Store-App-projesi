@page
@model StoreApp.Pages.DemoModel
@{
}

<div class="my-5 text-center">
	<div class="display-6">Merhaba @Model?.FullName</div>
	<p class="text-danger">@HttpContext.Session.GetString("name")</p>
	<form method="post" asp-asp-page="/demo">
		<div class="form-group">
			<input class="form-control" name="name" placeholder="Adınızı Girin" />
			<input type="submit" class="btn btn-primary my-3" />
		</div>
	</form>
</div>